services:
  - type: web
    name: editscape-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backend.app:create_app() --bind 0.0.0.0:$PORT
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 10000
